server:
  port: 8080

spring:
  application:
    name: api-gateway
  
  cloud:
    gateway:
      routes:
        # Market Data Service
        - id: market-data-service
          uri: ${MARKET_DATA_SERVICE_URL:http://localhost:8081}
          predicates:
            - Path=/api/market-data/**
          filters:
            - StripPrefix=0
        
        # Option Chain Service
        - id: option-chain-service
          uri: ${OPTION_CHAIN_SERVICE_URL:http://localhost:8082}
          predicates:
            - Path=/api/option-chain/**
          filters:
            - StripPrefix=0
        
        # Risk Service
        - id: risk-service
          uri: ${RISK_SERVICE_URL:http://localhost:8083}
          predicates:
            - Path=/api/risk/**
          filters:
            - StripPrefix=0
        
        # Journal Service
        - id: journal-service
          uri: ${JOURNAL_SERVICE_URL:http://localhost:8084}
          predicates:
            - Path=/api/journal/**
          filters:
            - StripPrefix=0
        
        # Quant Engine (with WebSocket support for Socket.io)
        - id: quant-engine
          uri: ${QUANT_ENGINE_URL:http://quant-engine:8001}
          predicates:
            - Path=/api/quant/**
          filters:
            - StripPrefix=0
        
        # Quant Engine WebSocket (Socket.io)
        - id: quant-engine-websocket
          uri: ${QUANT_ENGINE_WS_URL:ws://quant-engine:8001}
          predicates:
            - Path=/socket.io/**
          filters:
            - StripPrefix=0
        
        # AI Reasoning Service
        - id: ai-reasoning-service
          uri: ${AI_REASONING_SERVICE_URL:http://localhost:8002}
          predicates:
            - Path=/api/ai/**
          filters:
            - StripPrefix=0            
        # FYERS Data Bridge (Real-time Market Data)
        - id: fyers-bridge
          uri: http://fyers-bridge:8005
          predicates:
            - Path=/api/fyers/**
          filters:
            - StripPrefix=2      
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin
      
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            exposedHeaders: "*"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,gateway

logging:
  level:
    root: INFO
    org.springframework.cloud.gateway: DEBUG
